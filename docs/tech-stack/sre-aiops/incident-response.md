# 事件响应

> 目标：不是知道“故障要处理”，而是能在告警响起后快速声明事件、分级、分配角色、建立时间线、控制沟通节奏、优先缓解用户影响、升级和交接，并把现场记录沉淀成复盘、runbook 和 AIOps 数据。

## 官方资料

优先读这些官方资料：

- [Google SRE Book - Emergency Response](https://sre.google/sre-book/emergency-response/)
- [Google SRE Book - Managing Incidents](https://sre.google/sre-book/managing-incidents/)
- [Google SRE Workbook - Incident Response](https://sre.google/workbook/incident-response/)
- [Google SRE Book - Postmortem Culture: Learning from Failure](https://sre.google/sre-book/postmortem-culture/)
- [Google SRE Book - Effective Troubleshooting](https://sre.google/sre-book/effective-troubleshooting/)
- [Google SRE Workbook - Postmortem Culture](https://sre.google/workbook/postmortem-culture/)

说明：本文基于 Google SRE 的 emergency response、incident command、incident management 和 postmortem 资料，整理成适合 AIOps 知识库落地的事件响应教程。

## 场景开场

线上故障已经发生：

```text
order-api 5xx 错误率 23%
p95 延迟 1800ms
09:02 刚发布过新版本
用户反馈无法下单
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>order-api 5xx 错误率 23%</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>p95 延迟 1800ms</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>09:02 刚发布过新版本</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 4 行 | <code>用户反馈无法下单</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


群里十几个人同时说话：

- 有人说先回滚。
- 有人说先看数据库。
- 有人问影响多少用户。
- 有人追问谁发布的。
- 有人贴日志。
- 有人开始解释根因。
- 没有人记录时间线。

这就是事件响应要解决的问题：不是某一条命令怎么敲，而是让人、信息、动作和沟通在高压下有序起来。

## 一句话人话版

事件响应是故障发生后的组织方法：先明确谁指挥、谁排障、谁沟通、谁记录，再围绕用户影响快速缓解，持续同步状态，最后把过程沉淀成复盘和改进项。

## 小白可能会问

- incident 和普通告警有什么区别？
- incident commander 到底负责什么？
- 为什么故障中不能所有人同时指挥？
- 为什么要先缓解，而不是先追根因？
- 时间线应该记什么？
- SEV1、SEV2、SEV3 怎么分？
- 沟通更新怎么写才不乱？
- 什么时候升级、什么时候拉更多人？
- 事件恢复后是不是就结束了？
- 事件响应材料怎么变成 AIOps 数据？

## 官方知识地图

事件响应可以按这张地图理解：

```text
Detection
  -> alert / user report / synthetic check
  -> triage
  -> incident declaration
  -> severity
  -> roles
     -> Incident Commander
     -> Operations Lead
     -> Communications Lead
     -> Scribe
     -> Subject Matter Experts
  -> incident doc
     -> status
     -> impact
     -> timeline
     -> hypotheses
     -> actions
     -> decisions
  -> mitigation
     -> rollback
     -> failover
     -> disable feature
     -> rate limit
     -> traffic shift
  -> communication
     -> internal updates
     -> stakeholder updates
     -> customer-facing updates
  -> resolution
     -> monitoring
     -> handoff
     -> postmortem
  -> learning
     -> action items
     -> runbook updates
     -> alert improvements
     -> automation
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>Detection</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>  -&gt; alert / user report / synthetic check</code> | 这一行要理解这些英文词：`alert` 是告警；`user report` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题；`synthetic check` 是check=检查。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 3 行 | <code>  -&gt; triage</code> | 这一行要理解这些英文词：`triage` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 4 行 | <code>  -&gt; incident declaration</code> | 这一行要理解这些英文词：`incident declaration` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 5 行 | <code>  -&gt; severity</code> | 这一行要理解这些英文词：`severity` 是严重级别。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 6 行 | <code>  -&gt; roles</code> | 这一行要理解这些英文词：`roles` 是角色或职责。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 7 行 | <code>     -&gt; Incident Commander</code> | 这一行要理解这些英文词：`Incident Commander` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 8 行 | <code>     -&gt; Operations Lead</code> | 这一行要理解这些英文词：`Operations Lead` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 9 行 | <code>     -&gt; Communications Lead</code> | 这一行要理解这些英文词：`Communications Lead` 是communications=沟通记录。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 10 行 | <code>     -&gt; Scribe</code> | 这一行要理解这些英文词：`Scribe` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 11 行 | <code>     -&gt; Subject Matter Experts</code> | 这一行要理解这些英文词：`Subject Matter Experts` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 12 行 | <code>  -&gt; incident doc</code> | 这一行要理解这些英文词：`incident doc` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 13 行 | <code>     -&gt; status</code> | 这一行要理解这些英文词：`status` 是状态，表示资源、服务或任务当前处于什么情况。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 14 行 | <code>     -&gt; impact</code> | 这一行要理解这些英文词：`impact` 是影响范围。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 15 行 | <code>     -&gt; timeline</code> | 这一行要理解这些英文词：`timeline` 是时间线。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 16 行 | <code>     -&gt; hypotheses</code> | 这一行要理解这些英文词：`hypotheses` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 17 行 | <code>     -&gt; actions</code> | 这一行要理解这些英文词：`actions` 是动作或改进项。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 18 行 | <code>     -&gt; decisions</code> | 这一行要理解这些英文词：`decisions` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 19 行 | <code>  -&gt; mitigation</code> | 这一行要理解这些英文词：`mitigation` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 20 行 | <code>     -&gt; rollback</code> | 这一行要理解这些英文词：`rollback` 是回滚。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 21 行 | <code>     -&gt; failover</code> | 这一行要理解这些英文词：`failover` 是故障切换。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 22 行 | <code>     -&gt; disable feature</code> | 这一行要理解这些英文词：`disable feature` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 23 行 | <code>     -&gt; rate limit</code> | 这一行要理解这些英文词：`rate limit` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 24 行 | <code>     -&gt; traffic shift</code> | 这一行要理解这些英文词：`traffic shift` 是traffic=流量。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 25 行 | <code>  -&gt; communication</code> | 这一行要理解这些英文词：`communication` 是沟通。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 26 行 | <code>     -&gt; internal updates</code> | 这一行要理解这些英文词：`internal updates` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 27 行 | <code>     -&gt; stakeholder updates</code> | 这一行要理解这些英文词：`stakeholder updates` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 28 行 | <code>     -&gt; customer-facing updates</code> | 这一行要理解这些英文词：`customer-facing updates` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 29 行 | <code>  -&gt; resolution</code> | 这一行要理解这些英文词：`resolution` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 30 行 | <code>     -&gt; monitoring</code> | 这一行要理解这些英文词：`monitoring` 是监控。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 31 行 | <code>     -&gt; handoff</code> | 这一行要理解这些英文词：`handoff` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 32 行 | <code>     -&gt; postmortem</code> | 这一行要理解这些英文词：`postmortem` 是事故复盘报告。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 33 行 | <code>  -&gt; learning</code> | 这一行要理解这些英文词：`learning` 是学习沉淀。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 34 行 | <code>     -&gt; action items</code> | 这一行要理解这些英文词：`action items` 是后续改进事项，事故复盘后必须有人负责跟进的任务。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 35 行 | <code>     -&gt; runbook updates</code> | 这一行要理解这些英文词：`runbook updates` 是runbook=故障处理手册。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 36 行 | <code>     -&gt; alert improvements</code> | 这一行要理解这些英文词：`alert improvements` 是alert=告警。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 37 行 | <code>     -&gt; automation</code> | 这一行要理解这些英文词：`automation` 是自动化。`->` 只是知识地图里的层级符号，不是要学习的概念。 |


初学路线：

```text
declare early
  -> assign roles
  -> write status doc
  -> state impact
  -> mitigate first
  -> update every 15 minutes
  -> record decisions
  -> resolve and monitor
  -> create postmortem inputs
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>declare early</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>  -&gt; assign roles</code> | 这一行要理解这些英文词：`assign roles` 是roles=角色或职责。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 3 行 | <code>  -&gt; write status doc</code> | 这一行要理解这些英文词：`write status doc` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 4 行 | <code>  -&gt; state impact</code> | 这一行要理解这些英文词：`state impact` 是state=状态，impact=影响范围。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 5 行 | <code>  -&gt; mitigate first</code> | 这一行要理解这些英文词：`mitigate first` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 6 行 | <code>  -&gt; update every 15 minutes</code> | 这一行要理解这些英文词：`update every` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题；`minutes` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 7 行 | <code>  -&gt; record decisions</code> | 这一行要理解这些英文词：`record decisions` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 8 行 | <code>  -&gt; resolve and monitor</code> | 这一行要理解这些英文词：`resolve and monitor` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 9 行 | <code>  -&gt; create postmortem inputs</code> | 这一行要理解这些英文词：`create postmortem inputs` 是postmortem=事故复盘报告，inputs=输入值。`->` 只是知识地图里的层级符号，不是要学习的概念。 |


## 什么是 Incident

Incident 是正在或已经造成服务影响、需要协调响应的问题。

它不只是“有一条告警”。

| 事件 | 是否一定是 incident | 说明 |
|---|---:|---|
| CPU 高 | 不一定 | 可能无用户影响 |
| SLO 快速燃烧 | 通常是 | 已经或即将影响用户 |
| 用户无法下单 | 是 | 核心功能受损 |
| 单实例重启 | 不一定 | 服务冗余足够时可能只是噪声 |
| 数据丢失风险 | 是 | 影响严重，需要协调 |

一个简单判断：

```text
需要跨人协作、持续跟踪、沟通和记录吗？
需要 -> incident
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>需要跨人协作、持续跟踪、沟通和记录吗？</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>需要 -&gt; incident</code> | 这一行要理解这些英文词：`incident` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |


## 事件响应原则

Google SRE Emergency Response 的核心提醒很朴素：事情会坏，关键是人如何回应。正确回应需要准备、训练、测试和持续改进。

事件中最重要的原则：

| 原则 | 含义 |
|---|---|
| 不慌 | 先建立秩序，不在群里乱冲 |
| 早声明 | 不确定也可以先声明 incident |
| 角色清楚 | 一个现场只能有一个 IC |
| 缓解优先 | 先降低用户影响，再深挖根因 |
| 记录事实 | 时间线和决策要实时记 |
| 沟通节奏 | 定期更新，不让信息真空 |
| 快速拉人 | 不要独自扛超出能力的问题 |
| 事后学习 | 恢复后要复盘和改进 |

## 事件生命周期

```text
detected
  -> triaged
  -> declared
  -> investigating
  -> mitigating
  -> monitoring
  -> resolved
  -> postmortem
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>detected</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>  -&gt; triaged</code> | 这一行要理解这些英文词：`triaged` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 3 行 | <code>  -&gt; declared</code> | 这一行要理解这些英文词：`declared` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 4 行 | <code>  -&gt; investigating</code> | 这一行要理解这些英文词：`investigating` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 5 行 | <code>  -&gt; mitigating</code> | 这一行要理解这些英文词：`mitigating` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 6 行 | <code>  -&gt; monitoring</code> | 这一行要理解这些英文词：`monitoring` 是监控。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 7 行 | <code>  -&gt; resolved</code> | 这一行要理解这些英文词：`resolved` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 8 行 | <code>  -&gt; postmortem</code> | 这一行要理解这些英文词：`postmortem` 是事故复盘报告。`->` 只是知识地图里的层级符号，不是要学习的概念。 |


| 状态 | 含义 | 关键输出 |
|---|---|---|
| detected | 发现异常 | 告警或用户反馈 |
| triaged | 初步判断 | 是否 incident、初步影响 |
| declared | 声明事件 | 级别、IC、频道、文档 |
| investigating | 调查中 | 假设、证据、负责人 |
| mitigating | 缓解中 | 回滚、限流、切流、降级 |
| monitoring | 观察恢复 | 指标回落、用户影响消失 |
| resolved | 已恢复 | 结束时间、恢复证据 |
| postmortem | 复盘中 | 时间线、根因、行动项 |

状态变化都要写时间。

## 角色分工

最小角色模型：

| 角色 | 职责 |
|---|---|
| IC Incident Commander | 总指挥，维护全局状态，分配任务，做决策 |
| OL Operations Lead | 技术排障和缓解动作负责人 |
| CL Communications Lead | 对内对外沟通、状态更新 |
| Scribe | 记录时间线、决策、命令、结果 |
| SME Subject Matter Expert | 某个系统的专家，提供支持 |

小团队可以一人兼多角，但要意识到这些职责都存在。

### IC 不应该做什么

IC 不应该陷进具体命令里。

IC 应该关注：

- 当前用户影响是什么？
- 谁在做哪件事？
- 下一步最重要动作是什么？
- 是否需要升级？
- 什么时候下一次更新？
- 是否已经有缓解方案？

如果 IC 自己低头查日志，现场很容易失去节奏。

### OL 做什么

OL 负责技术动作：

- 查 dashboard。
- 查日志。
- 查变更。
- 验证假设。
- 执行或协调回滚、切流、降级。
- 汇报结果给 IC。

### CL 做什么

CL 负责让干系人知道真实状态：

- 内部值班频道更新。
- 业务方更新。
- 客服或客户成功同步。
- 状态页内容。
- 下一次更新时间。

CL 不需要解释所有技术细节，但要准确表达影响、行动和时间。

### Scribe 做什么

Scribe 记录：

- 告警触发时间。
- incident 声明时间。
- 角色分配。
- 假设。
- 命令和动作。
- 结果。
- 决策。
- 沟通更新。
- 恢复时间。

没有时间线，复盘会变成靠记忆争论。

## 严重级别

先用简单分级：

| 级别 | 用户影响 | 响应要求 |
|---|---|---|
| SEV1 | 大面积不可用、核心链路中断、数据安全风险 | 立即响应，持续指挥和沟通 |
| SEV2 | 重要功能受影响，部分用户受损，有绕行或缓解 | 快速响应，定期更新 |
| SEV3 | 小范围影响、内部问题、无明显用户影响 | 工单处理，可排期 |

示例：

```text
SEV1: 支付不可用，超过 30% 下单失败。
SEV2: order-api 错误率升高，约 8% 用户受影响。
SEV3: 后台报表延迟 30 分钟，用户主链路无影响。
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>SEV1: 支付不可用，超过 30% 下单失败。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>SEV2: order-api 错误率升高，约 8% 用户受影响。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>SEV3: 后台报表延迟 30 分钟，用户主链路无影响。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


分级要和组织实际匹配。不要把所有故障都定 SEV1，否则 SEV1 会失去意义。

## 声明 Incident

不要等根因清楚才声明。

声明 incident 的价值：

- 建立指挥链。
- 创建统一频道。
- 创建统一文档。
- 避免多头指挥。
- 让干系人知道哪里看状态。

声明模板：

```text
声明 SEV2 incident：order-api HighErrorRate。
当前影响：约 12% 请求返回 5xx，用户下单受影响。
IC：Alice
OL：Bob
CL：Carol
Scribe：Dave
频道：#inc-20260702-order-api
文档：INC-2026-0702-001
下一次更新：09:30
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>声明 SEV2 incident：order-api HighErrorRate。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>当前影响：约 12% 请求返回 5xx，用户下单受影响。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>IC：Alice</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 4 行 | <code>OL：Bob</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 5 行 | <code>CL：Carol</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 6 行 | <code>Scribe：Dave</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 7 行 | <code>频道：#inc-20260702-order-api</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 8 行 | <code>文档：INC-2026-0702-001</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 9 行 | <code>下一次更新：09:30</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


## Incident 文档模板

`incident.md`：

```md
# Incident: order-api HighErrorRate

## 基本信息

- ID: INC-2026-0702-001
- 级别: SEV2
- 服务: order-api
- 开始时间: 2026-07-02 09:10
- 发现方式: Alertmanager OrderApiHighErrorRate
- IC:
- OL:
- CL:
- Scribe:
- 频道:
- 文档负责人:

## 当前状态

- 状态: investigating
- 用户影响:
- 影响范围:
- 当前缓解:
- 下一次更新时间:

## 时间线

| 时间 | 事件 | 负责人 | 证据 |
|---|---|---|---|
| 09:10 | OrderApiHighErrorRate 触发 | oncall | Alertmanager |
| 09:12 | 声明 SEV2 incident | IC | 频道公告 |

## 假设和验证

| 假设 | 验证方式 | 负责人 | 结果 |
|---|---|---|---|
| 最近发布导致错误率升高 | 对比发布前后日志和指标 | OL | 待确认 |

## 决策记录

| 时间 | 决策 | 决策人 | 原因 | 风险 |
|---|---|---|---|---|
| 09:25 | 准备回滚 order-api | IC | 5xx 持续升高且发布相关 | 中 |

## 缓解动作

| 动作 | 负责人 | 风险 | 审批 | 结果 |
|---|---|---|---|---|
| 回滚 order-api 到上一版本 | OL | 中 | 需要 | 待执行 |

## 沟通记录

| 时间 | 渠道 | 内容 |
|---|---|---|
| 09:15 | 内部频道 | 已声明 SEV2，正在确认发布影响 |

## 恢复标准

- 5xx 错误率回到基线。
- p95 延迟回到基线。
- SLO burn rate 恢复到可接受范围。
- 用户反馈停止增加。

## 后续事项

- [ ] 复盘 RCA
- [ ] 更新 order-api runbook
- [ ] 修复告警描述
- [ ] 补充回滚演练
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code># Incident: order-api HighErrorRate</code> | Markdown 标题行，用来组织文档层级。 |
| 第 2 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 3 行 | <code>## 基本信息</code> | Markdown 标题行，用来组织文档层级。 |
| 第 4 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 5 行 | <code>- ID: INC-2026-0702-001</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 6 行 | <code>- 级别: SEV2</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 7 行 | <code>- 服务: order-api</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 8 行 | <code>- 开始时间: 2026-07-02 09:10</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 9 行 | <code>- 发现方式: Alertmanager OrderApiHighErrorRate</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 10 行 | <code>- IC:</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 11 行 | <code>- OL:</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 12 行 | <code>- CL:</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 13 行 | <code>- Scribe:</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 14 行 | <code>- 频道:</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 15 行 | <code>- 文档负责人:</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 16 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 17 行 | <code>## 当前状态</code> | Markdown 标题行，用来组织文档层级。 |
| 第 18 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 19 行 | <code>- 状态: investigating</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 20 行 | <code>- 用户影响:</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 21 行 | <code>- 影响范围:</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 22 行 | <code>- 当前缓解:</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 23 行 | <code>- 下一次更新时间:</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 24 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 25 行 | <code>## 时间线</code> | Markdown 标题行，用来组织文档层级。 |
| 第 26 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 27 行 | <code>&#124; 时间 &#124; 事件 &#124; 负责人 &#124; 证据 &#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 28 行 | <code>&#124;---&#124;---&#124;---&#124;---&#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 29 行 | <code>&#124; 09:10 &#124; OrderApiHighErrorRate 触发 &#124; oncall &#124; Alertmanager &#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 30 行 | <code>&#124; 09:12 &#124; 声明 SEV2 incident &#124; IC &#124; 频道公告 &#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 31 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 32 行 | <code>## 假设和验证</code> | Markdown 标题行，用来组织文档层级。 |
| 第 33 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 34 行 | <code>&#124; 假设 &#124; 验证方式 &#124; 负责人 &#124; 结果 &#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 35 行 | <code>&#124;---&#124;---&#124;---&#124;---&#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 36 行 | <code>&#124; 最近发布导致错误率升高 &#124; 对比发布前后日志和指标 &#124; OL &#124; 待确认 &#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 37 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 38 行 | <code>## 决策记录</code> | Markdown 标题行，用来组织文档层级。 |
| 第 39 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 40 行 | <code>&#124; 时间 &#124; 决策 &#124; 决策人 &#124; 原因 &#124; 风险 &#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 41 行 | <code>&#124;---&#124;---&#124;---&#124;---&#124;---&#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 42 行 | <code>&#124; 09:25 &#124; 准备回滚 order-api &#124; IC &#124; 5xx 持续升高且发布相关 &#124; 中 &#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 43 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 44 行 | <code>## 缓解动作</code> | Markdown 标题行，用来组织文档层级。 |
| 第 45 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 46 行 | <code>&#124; 动作 &#124; 负责人 &#124; 风险 &#124; 审批 &#124; 结果 &#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 47 行 | <code>&#124;---&#124;---&#124;---&#124;---&#124;---&#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 48 行 | <code>&#124; 回滚 order-api 到上一版本 &#124; OL &#124; 中 &#124; 需要 &#124; 待执行 &#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 49 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 50 行 | <code>## 沟通记录</code> | Markdown 标题行，用来组织文档层级。 |
| 第 51 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 52 行 | <code>&#124; 时间 &#124; 渠道 &#124; 内容 &#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 53 行 | <code>&#124;---&#124;---&#124;---&#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 54 行 | <code>&#124; 09:15 &#124; 内部频道 &#124; 已声明 SEV2，正在确认发布影响 &#124;</code> | Markdown 表格行，用来对齐展示字段和说明。 |
| 第 55 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 56 行 | <code>## 恢复标准</code> | Markdown 标题行，用来组织文档层级。 |
| 第 57 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 58 行 | <code>- 5xx 错误率回到基线。</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 59 行 | <code>- p95 延迟回到基线。</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 60 行 | <code>- SLO burn rate 恢复到可接受范围。</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 61 行 | <code>- 用户反馈停止增加。</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 62 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 63 行 | <code>## 后续事项</code> | Markdown 标题行，用来组织文档层级。 |
| 第 64 行 | <em>空行</em> | 空行，用来分隔 Markdown 段落。 |
| 第 65 行 | <code>- [ ] 复盘 RCA</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 66 行 | <code>- [ ] 更新 order-api runbook</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 67 行 | <code>- [ ] 修复告警描述</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |
| 第 68 行 | <code>- [ ] 补充回滚演练</code> | Markdown 列表项，用来列出步骤、要点或证据清单。 |


## 时间线怎么记

时间线记录事实，不记录情绪。

要记：

- 告警触发。
- 用户反馈。
- 关键指标变化。
- 事件声明。
- 角色分配。
- 假设提出。
- 验证结果。
- 缓解动作。
- 沟通更新。
- 恢复证据。

不要只记：

```text
大家开始排查。
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>大家开始排查。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


更好的写法：

```text
09:18 OL 开始对比 order-api v2026.07.02.1 与上一版本的 5xx 错误日志。
09:23 日志显示新版本在 createOrder 接口出现 database timeout，占 5xx 的 78%。
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>09:18 OL 开始对比 order-api v2026.07.02.1 与上一版本的 5xx 错误日志。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>09:23 日志显示新版本在 createOrder 接口出现 database timeout，占 5xx 的 78%。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


## 排障流程

先问四个问题：

1. 用户影响是什么？
2. 影响范围多大？
3. 从什么时候开始？
4. 那之前发生了什么变化？

推荐顺序：

```text
SLO / symptom dashboard
  -> recent changes
  -> error logs
  -> dependency status
  -> saturation
  -> rollback / failover / degrade
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>SLO / symptom dashboard</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>  -&gt; recent changes</code> | 这一行要理解这些英文词：`recent changes` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 3 行 | <code>  -&gt; error logs</code> | 这一行要理解这些英文词：`error logs` 是error=错误，logs=日志。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 4 行 | <code>  -&gt; dependency status</code> | 这一行要理解这些英文词：`dependency status` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 5 行 | <code>  -&gt; saturation</code> | 这一行要理解这些英文词：`saturation` 是英文关键字，表示本节知识地图里的一个组件、命令、状态或学习主题。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 6 行 | <code>  -&gt; rollback / failover / degrade</code> | 这一行要理解这些英文词：`rollback` 是回滚；`failover` 是故障切换；`degrade` 是降级。`->` 只是知识地图里的层级符号，不是要学习的概念。 |


不要一开始就沉迷根因细节。事件中最重要的是降低用户影响。

## 缓解优先

事件响应里，根因分析很重要，但不是第一优先级。

优先级：

```text
protect users
  -> stabilize service
  -> preserve evidence
  -> understand root cause
  -> prevent recurrence
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>protect users</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>  -&gt; stabilize service</code> | 这一行要理解这些英文词：`stabilize service` 是稳定服务，先止血恢复用户影响，再做深入复盘。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 3 行 | <code>  -&gt; preserve evidence</code> | 这一行要理解这些英文词：`preserve evidence` 是保留证据，先留住日志、指标、变更和时间线，避免事故后查不到。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 4 行 | <code>  -&gt; understand root cause</code> | 这一行要理解这些英文词：`understand root cause` 是root=根因或根模块。`->` 只是知识地图里的层级符号，不是要学习的概念。 |
| 第 5 行 | <code>  -&gt; prevent recurrence</code> | 这一行要理解这些英文词：`prevent recurrence` 是防止复发，把复盘结论落成监控、流程、代码或架构改进。`->` 只是知识地图里的层级符号，不是要学习的概念。 |


常见缓解动作：

| 动作 | 适用场景 | 风险 |
|---|---|---|
| 回滚 | 最近发布高度相关 | 可能回滚其他修复 |
| 切流 | 单区域或单集群异常 | 容量压力转移 |
| 限流 | 过载或依赖保护 | 用户请求被拒 |
| 降级 | 非核心功能拖垮核心链路 | 功能体验下降 |
| 关闭 feature flag | 新功能导致故障 | 影响新功能 |
| 扩容 | 资源不足 | 成本和启动时间 |
| 禁用自动化 | 自动化扩大影响 | 人工负担增加 |

缓解动作要记录：

- 谁决定。
- 为什么。
- 风险是什么。
- 是否审批。
- 执行时间。
- 结果如何。

## 沟通节奏

事件中没有更新，会让人默认“没人管”。

SEV1/SEV2 建议固定节奏：

| 级别 | 更新频率 |
|---|---|
| SEV1 | 5 到 15 分钟 |
| SEV2 | 15 到 30 分钟 |
| SEV3 | 视工单节奏 |

状态更新模板：

```text
[09:30] order-api SEV2 更新
当前影响：约 12% 请求返回 5xx，主要影响下单接口。
已确认：错误率从 09:10 开始升高，09:02 有一次 order-api 发布。
正在做：OL 正在准备回滚并评估风险。
下一步：09:35 前完成回滚决策。
下一次更新：09:45。
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>[09:30] order-api SEV2 更新</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>当前影响：约 12% 请求返回 5xx，主要影响下单接口。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>已确认：错误率从 09:10 开始升高，09:02 有一次 order-api 发布。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 4 行 | <code>正在做：OL 正在准备回滚并评估风险。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 5 行 | <code>下一步：09:35 前完成回滚决策。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 6 行 | <code>下一次更新：09:45。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


原则：

- 说事实，不说猜测。
- 明确影响。
- 明确正在做什么。
- 明确下一次更新时间。
- 不确定就写不确定。

## 升级和拉人

不要独自扛太久。

需要升级的信号：

- 用户影响扩大。
- 15 到 30 分钟没有进展。
- 需要高权限操作。
- 涉及多个团队。
- 怀疑数据丢失或安全风险。
- 当前值班人员不熟悉系统。
- 自动化或回滚失败。

拉人时要带上下文：

```text
需要数据库 SME 支持。
事件：INC-2026-0702-001 order-api SEV2。
问题：5xx 中 78% 是 database timeout。
已查：发布相关性高，但回滚前需要确认数据库连接数是否异常。
需要你：确认 09:00-09:30 数据库连接池和慢查询状态。
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>需要数据库 SME 支持。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>事件：INC-2026-0702-001 order-api SEV2。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>问题：5xx 中 78% 是 database timeout。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 4 行 | <code>已查：发布相关性高，但回滚前需要确认数据库连接数是否异常。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 5 行 | <code>需要你：确认 09:00-09:30 数据库连接池和慢查询状态。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


不要只发：

```text
快来看看数据库。
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>快来看看数据库。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


## 交接

长事件需要跨班交接。

交接必须写清楚：

- 当前状态。
- 用户影响。
- 已执行动作。
- 未完成动作。
- 当前假设。
- 风险点。
- 下一步。
- 联系人。

交接模板：

```text
交接给 EU on-call：
状态：monitoring，错误率已从 23% 降到 0.4%，仍高于基线。
已做：09:42 回滚 order-api 到 v2026.07.01.3。
待做：继续观察 1 小时；如果错误率再次升高，检查 payment-api 依赖。
风险：数据库连接池指标仍有轻微抖动。
文档：INC-2026-0702-001。
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>交接给 EU on-call：</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>状态：monitoring，错误率已从 23% 降到 0.4%，仍高于基线。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>已做：09:42 回滚 order-api 到 v2026.07.01.3。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 4 行 | <code>待做：继续观察 1 小时；如果错误率再次升高，检查 payment-api 依赖。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 5 行 | <code>风险：数据库连接池指标仍有轻微抖动。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 6 行 | <code>文档：INC-2026-0702-001。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


没有交接的跨时区事件，很容易重复排查或漏掉关键风险。

## 何时 Resolve

不要刚看到指标下降就立即关闭。

恢复标准要提前写清楚：

- 关键 SLI 回到可接受范围。
- 错误预算燃烧停止。
- 业务反馈恢复。
- 缓解动作完成。
- 没有新的高风险异常。
- 负责人同意进入 monitoring 或 resolved。

resolved 后还要：

- 通知干系人。
- 保留 incident 文档。
- 开始 postmortem。
- 追踪后续 action items。

## 事件响应和 RCA 的区别

| 阶段 | 目标 | 关注点 |
|---|---|---|
| 事件响应 | 恢复服务 | 用户影响、缓解、沟通 |
| RCA / 复盘 | 防止复发 | 根因、触发因素、系统性改进 |

事件中可以记录候选根因，但不要让“证明根因”阻碍缓解。

正确顺序：

```text
先让用户恢复。
再解释为什么坏。
最后改系统防止再坏。
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>先让用户恢复。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>再解释为什么坏。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>最后改系统防止再坏。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


## AIOps 中的作用

事件响应材料是 AIOps 的高价值数据。

可沉淀字段：

| 数据 | AIOps 用途 |
|---|---|
| 告警时间 | 事件检测训练 |
| 声明时间 | MTTA 分析 |
| 缓解时间 | MTTR 分析 |
| 服务和标签 | 告警聚类 |
| 时间线 | RCA 辅助 |
| 假设和验证 | 根因候选库 |
| 缓解动作 | runbook 推荐 |
| 沟通更新 | 自动摘要训练 |
| action items | 改进追踪 |

AIOps 可以辅助：

- 汇总告警组。
- 补全上下文。
- 推荐 runbook。
- 检索相似事故。
- 生成状态更新草稿。
- 生成 postmortem 初稿。

但高风险决策仍然要由人确认。

## 入门练习：模拟一次 SEV2

场景：

```text
09:10 order-api 5xx 错误率升到 23%
09:02 order-api 刚发布 v2026.07.02.1
p95 延迟从 120ms 升到 1800ms
日志出现 database connection timeout
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>09:10 order-api 5xx 错误率升到 23%</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>09:02 order-api 刚发布 v2026.07.02.1</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>p95 延迟从 120ms 升到 1800ms</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 4 行 | <code>日志出现 database connection timeout</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


要求：

1. 声明 SEV2。
2. 指定 IC、OL、CL、Scribe。
3. 建立 incident 文档。
4. 写 5 条时间线。
5. 写 2 个假设和验证方式。
6. 写 1 条状态更新。
7. 写 2 个缓解动作和风险。
8. 写 resolved 标准。
9. 写复盘待办。

完成后，你应该得到：

```text
INC-2026-0702-001-order-api.md
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>INC-2026-0702-001-order-api.md</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


这就是作品集里非常有价值的 SRE/AIOps 材料。

## 常见错误

### 一开始就追根因

根因重要，但用户还在受影响时，先缓解。

### 没有人指挥

所有人都说话等于没人指挥。必须有 IC。

### IC 深入执行命令

IC 应该维护全局状态，不应该陷入单个终端。

### 没有时间线

没有时间线，复盘只能靠记忆。

### 沟通缺失

技术排障做得再好，干系人不知道进展也会焦虑。

### 过早 resolved

指标刚恢复不等于事件结束。需要观察和明确恢复标准。

### 不做复盘

恢复服务只是第一步。没有复盘，就没有组织学习。

## 常用模板字典

### 声明模板

```text
声明 {severity} incident：{service} {symptom}。
当前影响：{impact}。
IC：{name}
OL：{name}
CL：{name}
Scribe：{name}
频道：{channel}
文档：{doc}
下一次更新：{time}
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>声明 {severity} incident：{service} {symptom}。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>当前影响：{impact}。</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>IC：{name}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 4 行 | <code>OL：{name}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 5 行 | <code>CL：{name}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 6 行 | <code>Scribe：{name}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 7 行 | <code>频道：{channel}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 8 行 | <code>文档：{doc}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 9 行 | <code>下一次更新：{time}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


### 状态更新模板

```text
[{time}] {service} {severity} 更新
当前影响：{impact}
已确认：{facts}
正在做：{current_actions}
下一步：{next_steps}
下一次更新：{next_update_time}
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>[{time}] {service} {severity} 更新</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>当前影响：{impact}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>已确认：{facts}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 4 行 | <code>正在做：{current_actions}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 5 行 | <code>下一步：{next_steps}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 6 行 | <code>下一次更新：{next_update_time}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


### 缓解决策模板

```text
决策：{action}
原因：{evidence}
风险：{risk}
审批：{approval}
回滚方案：{rollback}
负责人：{owner}
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>决策：{action}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>原因：{evidence}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>风险：{risk}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 4 行 | <code>审批：{approval}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 5 行 | <code>回滚方案：{rollback}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 6 行 | <code>负责人：{owner}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


### 交接模板

```text
当前状态：{status}
用户影响：{impact}
已执行：{done}
待执行：{todo}
当前假设：{hypotheses}
风险：{risks}
联系人：{contacts}
文档：{doc}
```

逐行解释：

| 行 | 内容 | 说明 |
|---|---|---|
| 第 1 行 | <code>当前状态：{status}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 2 行 | <code>用户影响：{impact}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 3 行 | <code>已执行：{done}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 4 行 | <code>待执行：{todo}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 5 行 | <code>当前假设：{hypotheses}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 6 行 | <code>风险：{risks}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 7 行 | <code>联系人：{contacts}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |
| 第 8 行 | <code>文档：{doc}</code> | 文本示例行，用来展示输出、目录、流程、错误信息或学习证据中的一条内容。 |


## 面试怎么讲

事件响应是故障发生后的组织方法，核心不是立刻证明根因，而是快速建立指挥链、明确角色、记录时间线、降低用户影响并保持沟通。一般会有 Incident Commander 负责全局指挥，Operations Lead 负责技术排障和缓解，Communications Lead 负责状态更新，Scribe 负责记录事实和决策。

我会尽早声明 incident，按用户影响分 SEV，先看 SLO 和用户症状，再查最近变更、日志、依赖和资源饱和。处理过程中优先缓解，比如回滚、切流、限流、降级或关闭 feature flag，同时记录每个动作的原因、风险和结果。事件恢复后不能直接结束，要进入 monitoring、发送恢复通知，并把时间线、假设、缓解动作和遗留问题交给 postmortem、runbook 和 AIOps 知识库。

## 学习检查清单

- [ ] 我能解释 incident 和普通告警的区别。
- [ ] 我能说明为什么要尽早声明 incident。
- [ ] 我能说清 IC、OL、CL、Scribe 的职责。
- [ ] 我能设计 SEV1/SEV2/SEV3 分级。
- [ ] 我能写 incident 声明。
- [ ] 我能填写 incident 文档模板。
- [ ] 我能记录时间线、假设、决策和缓解动作。
- [ ] 我能写一条清晰状态更新。
- [ ] 我能解释为什么缓解优先于根因证明。
- [ ] 我能判断什么时候升级和拉人。
- [ ] 我能写交接模板。
- [ ] 我能说明事件响应材料如何进入 AIOps。

## 面试题

1. 什么是 incident？
2. incident 和 alert 有什么区别？
3. Incident Commander 负责什么？
4. 为什么事件中要有 Communications Lead？
5. 为什么要有 Scribe？
6. SEV1、SEV2、SEV3 如何区分？
7. 为什么要尽早声明 incident？
8. 为什么故障中先缓解而不是先追根因？
9. 时间线应该记录哪些内容？
10. 一条好的状态更新包含什么？
11. 什么时候应该升级或拉更多人？
12. 交接时必须交代哪些内容？
13. resolved 的标准是什么？
14. 事件响应和 RCA 有什么区别？
15. AIOps 如何利用 incident 文档？

## 学习证据

学完后，在 GitHub 留下这些证据：

- 一份模拟 SEV2 incident 文档。
- 一条 incident 声明消息。
- 至少 5 条时间线记录。
- 至少 2 个假设和验证结果。
- 至少 2 个缓解动作和风险说明。
- 一条状态更新。
- 一条交接记录。
- 一份 resolved 标准。
- README 说明 incident 数据如何沉淀到 AIOps 知识库。
