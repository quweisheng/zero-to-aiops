# 技术栈精讲写作标准

这个标准约束本仓库所有技术栈文章，也约束学习路线、面试准备、资料导读和项目模板中的教学内容。目标是让完全零基础的读者在老师带领下，逐步做到解释机制、完成实验、分析生产事故，并用自己的证据回答深入的面试追问。

## 2026-09 全站老师带学版要求

技术栈以接近一万字的有效正文为写作目标；领域较大时可以超过，不为控制篇幅省掉必要的概念和实验。篇幅较短且尚未覆盖核心机制的文章要继续扩写，不能仅添加几个标题就标成完成。一万字是内容预算，不能用重复话术、代码块、长命令输出、链接或通用检查单凑数。统计时同时记录中文正文字数和正文总字符数，代码及输出单列；“一万字符”不能宣称为“一万汉字”。路线和模板以指导行动完整为准，无须强行写成一万字。

### 先补前置知识，再带学生推理

每篇开头交代读者当前站在哪里、需要什么工具、今天能完成哪件事。第一次出现进程、端口、请求、文件路径、租户、时间序列、事务、张量等概念时，先说日常含义，再给准确技术定义。遇到必须单独学习的前置知识，提供本仓库的具体文章链接，并指出本实验实际需要哪一小部分。不能一句“先学完 Linux/Kubernetes”把零基础门槛转交给读者。

正文像老师在黑板前带学生走一条业务链：“你先观察这个现象”“我们把数据沿着这条路径追一遍”“现在暂停一下，试着预测下一步”。类比之后必须说明类比的边界，避免把主从、复制、缓存、消息重试、模型推理讲成错误的简化模型。每个关键问题给出因果推理，学生应当知道命令为什么排在这里、证据支持了什么、又排除了什么。

### 英文知识地图和其他模块都加中文注释

官方知识地图保留官方英文名称，紧邻加准确中文解释，例如 `Control plane（控制平面：决定系统应该变成什么状态）`。文章中其他架构图、状态机、请求路径和课程模块同样处理。正式 API、配置键、资源类型和命令保持原拼写；在注释或正文说明，不把可执行标识翻译成中文。

```text
Client（客户端：发出请求）
  -> Gateway（网关：校验并转发请求）
  -> Service（业务服务：处理请求）
  -> Database（数据库：持久保存业务记录）
```

图中箭头要说明代表请求、数据、控制指令还是依赖关系。不要用一张无说明的箭头图同时暗示四种关系。JSON、真实日志和必须逐字执行的命令不插入非法注释，改在紧邻正文解释字段。每章中的新缩写都遵守这一规则；不能只翻译第一张地图。

中文释义必须结合当前技术语境人工复核，不能靠通用词典整站替换。例如数据库的 `buffer pool` 是缓冲池，而 PyTorch 模块的 `buffer` 是不作为可训练参数的状态；数据库 `primary key` 是主键，分布式系统的 `primary` 可能是主副本；张量的 `rank` 可指轴数，分布式训练进程的 `rank` 则是进程序号。同一个英文词不能因此得到同一个固定译法。扫描时既看全英文图，也看混合图里的英文分支；补中文之后还要复核中文是否正确，不能以“含有汉字”当作验收。

### 课堂、实验和面试要接在一起

每篇安排一个可以预测结果的小问题、一项基础实验、一项可清理的故障注入或生产模拟。实验交代操作系统、运行位置、前置工具、文件名、完整命令、预期输出、验收和清理。示例输入输出要标为示例；只有实际执行且有记录的实验才能写“已验证”。商业软硬件不具备实验条件时，使用明确标注的纸面或合成数据演练，并讲清它验证的能力与没有覆盖的真实系统行为。

面试训练从“30 秒说明用途”推进到“3 分钟讲数据路径”，再追问设计取舍、反例、生产故障和方案设计。参考答案要解释为什么；练习复述自己的观察，不能编造生产经历。完成文章提供的是学习路径与训练材料，能否达到岗位要求还要通过独立操作、编码、系统设计和表达练习检验。

### 发布时报告真实覆盖范围

逐篇记录是否已补老师带学、地图中文释义、核心机制、两级实验和递进答案。篇幅统计和自动关键词检查只能提示缺口，不能代替技术审校或宣称掌握程度。全站更新必须同步导航、索引、搜索和线上验证；未达到篇幅或深度目标的条目明确列出，不能借一次构建成功把全部内容标记为终审完成。

## 参考站扫描结论

参考：[二哥的 Java 进阶之路：一文让你彻底了解 Java](https://javabetter.cn/overview/what-is-java.html)。

这个站点值得借鉴的不是具体人设和段子，而是它的讲解方法：

1. 先让读者进入一个真实的学习场景，再开始讲概念。
2. 用对话推进节奏，让新手把“不懂、疑惑、追问”说出来。
3. 每个术语第一次出现时，都用大白话拆一遍。
4. 先讲为什么，再讲是什么，最后讲怎么做。
5. 用历史、类比、截图、代码、命令和小实验交替推进，避免连续堆概念。
6. 一段只解决一个小问题，读者跟得上再进入下一段。
7. 结尾要落到应用场景、就业价值、学习路线或下一步行动。

本仓库要借鉴这种“有人带着学”的感觉，但不能照搬它的角色、原句、笑点和图片。AIOps 技术栈的主线应该是：

```text
运维真实问题
  -> 小白疑问
  -> 大白话解释
  -> 核心机制
  -> 最小实验
  -> 常见故障
  -> AIOps 场景
  -> 面试表达
  -> GitHub 学习证据
```

文本图、目录树和知识地图要保持简洁。需要解释英文缩写、组件名或字段名时，放在图前后的正文里说明，不要在每个示例块后面追加大段解释表格。

## 文章不是资料搬运

每篇文章都必须基于官方文档和实际学习场景重新组织。可以引用官方链接，可以引用少量配置片段，但不能复制官方全文，也不能把官网内容逐段翻译。

写作时优先回答三个问题：

1. 这个技术为什么和 AIOps 有关？
2. 一个运维小白学它，第一天应该掌握什么？
3. 学完以后能留下什么证据放到 GitHub？

## 固定结构

每个技术栈文件至少包含这些部分：

| 部分 | 目的 |
|---|---|
| 学习目标 | 告诉读者学完能做什么 |
| 官方资料 | 给出官网入口，避免资料来源混乱 |
| 场景开场 | 用一个运维或学习场景把读者带进来 |
| 一句话人话版 | 先用一句白话讲清楚它大概是什么 |
| 小白追问 | 主动写出新手最可能卡住的问题 |
| 为什么要学 | 连接运维、SRE、AIOps 和求职 |
| 是什么 | 用大白话讲定义 |
| 解决什么问题 | 解释它出现的原因 |
| 核心原理 | 讲机制，不只讲命令 |
| 架构和数据流 | 说明组件关系 |
| 安装与启动 | 让读者能跑起来 |
| 配置详解 | 解释配置项为什么这么写 |
| 常用命令或查询 | 给出入门必会命令 |
| AIOps 场景 | 说明它在指标、日志、告警、自动化、AI 中的位置 |
| 入门实验 | 给出可复现的小实验 |
| 常见故障排查 | 写现象、原因、检查、解决 |
| 学习检查清单 | 帮读者确认自己学会了什么 |
| 面试题 | 把知识转成表达能力 |
| 学习证据 | 指导读者把成果提交到 GitHub |

## 通俗讲解流程

每篇文章尽量按下面的节奏展开。

1. 场景开场：不要一上来定义术语。先写一个读者可能遇到的场景，比如“半夜告警响了，只知道 CPU 高，但不知道哪个进程惹的事”。
2. 一句话人话版：用一句不超过 80 字的话解释这个技术。
3. 新手追问：写出 2 到 4 个读者会问的问题，例如“为什么不能直接看日志？”“为什么容器里 localhost 不一样？”。
4. 术语拆解：第一次出现英文缩写、组件名、配置字段时，立刻解释它的作用。
5. 机制图解：能用文字图就画文字图，先把数据从哪里来到哪里去讲清楚。
6. 最小实验：给一条最短路径，让读者今天就能跑起来。
7. 排障回路：每个实验后面必须写“如果没成功，先看哪里”。
8. AIOps 落点：最后说明它能给异常检测、告警降噪、根因分析、自动化修复提供什么。
9. 面试表达：把知识点改写成可以开口说的回答。
10. 学习证据：明确这篇学完后应该提交什么文件、截图或笔记。

## 讲解颗粒度

不要只写：

```text
Prometheus 是监控系统，可以采集指标。
```

要写成：

```text
Prometheus 负责周期性访问目标服务的 /metrics 接口，把返回的数值样本保存为时间序列。每条时间序列由指标名和标签唯一确定，所以同一个 http_requests_total 指标可以按 method、status、instance 区分不同维度。
```

讲解要覆盖“定义、为什么、怎么工作、怎么配置、怎么验证、坏了怎么查”。

## 深讲硬性标准

“通俗”不是“浅”。后续每篇真正的精讲文章，必须同时满足下面这些要求，否则只能算入门提纲，不能算精讲。

### 1. 先给官方知识地图

文章要先说明这门技术的官方资料结构，告诉读者官方文档大概分成哪些部分，以及本篇会覆盖哪些部分。

例如 Linux 不能只写“会用 top、free、df”。它至少要讲清：

- Linux 内核是什么。
- 用户态和内核态怎么分工。
- 系统调用是什么。
- 进程、线程、调度、内存、文件系统、网络、设备、权限分别是什么。
- `/proc`、`/sys`、`/etc`、`/var/log` 这些目录为什么重要。
- 常用命令来自哪些用户态工具包，而不是都属于 Linux 内核。

### 2. 每个知识点都要按“五件套”讲

每个核心概念必须包含：

| 项 | 要求 |
|---|---|
| 是什么 | 一句话定义，不绕 |
| 为什么需要 | 它解决什么问题 |
| 怎么工作 | 数据流、调用链或状态变化 |
| 怎么看 / 怎么用 | 对应命令、配置、API 或页面 |
| 坏了怎么查 | 常见现象、检查顺序、修复思路 |

### 3. 命令必须像字典一样讲

命令不能只列名字。至少要写：

| 项 | 示例 |
|---|---|
| 命令 | `ps` |
| 作用 | 查看进程快照 |
| 常用语法 | `ps -ef`、`ps aux` |
| 关键字段 | PID、PPID、USER、STAT、CMD |
| 示例 | `ps -ef | grep nginx` |
| 预期输出 | 能看到进程号和启动命令 |
| AIOps 场景 | 告警后确认服务进程是否存在 |
| 常见坑 | `grep` 自己也会出现在结果里 |

### 4. 示例说明要克制

示例的目标是帮小白跑通和理解，不是给每个代码块都追加一张解释表。

硬性要求：

- 能用行内中文注释讲清楚的命令、SQL、YAML 和配置，优先写行内注释。
- JSON、终端输出、文本图这类不适合行内注释的内容，用正文或小表格解释关键字段即可。
- 示例前后要说明：它解决什么问题、怎么运行、正常会看到什么、失败先检查哪里。
- 不要为了“解释每一行”制造大量重复表格，影响阅读节奏。

SQL 示例应该这样写：

```sql
SELECT service_name, COUNT(*) AS alert_count -- 取服务名，并统计每个服务有多少条告警，统计结果命名为 alert_count
FROM alerts                                  -- 从 alerts 告警表里读取数据
WHERE created_at >= NOW() - INTERVAL 1 DAY   -- 只看最近 1 天产生的告警，避免把历史旧告警也算进去
GROUP BY service_name                        -- 按服务名分组，让每个服务单独统计
ORDER BY alert_count DESC                    -- 按告警数量从多到少排序，先看到最吵的服务
LIMIT 10;                                    -- 只返回前 10 个服务，适合做告警治理 TopN
```

YAML 示例应该这样写：

```yaml
scrape_interval: 15s  # Prometheus 每 15 秒抓一次指标；太短会增加压力，太长会让告警变慢
```

Bash 示例应该这样写：

```bash
docker ps # 查看正在运行的容器，确认实验服务是不是已经启动
```

JSON 示例不能写注释时，可以在正文或字段表里说明关键字段：

```json
{
  "service": "order-api",
  "severity": "critical"
}
```

### 5. 文章要有学习路径，不只是知识堆叠

每篇文章必须告诉小白按什么顺序学：

```text
先理解概念
  -> 再看架构
  -> 再学最小命令
  -> 再做实验
  -> 再学排障
  -> 最后整理 GitHub 证据
```

### 6. 明确边界和取舍

如果某个领域本身没有“全部命令”这种单一清单，文章要说清楚边界。

例如 Linux 命令来自 GNU coreutils、util-linux、procps-ng、iproute2、systemd、发行版包管理器等不同项目。文章可以定义“AIOps 入门必会命令全集”，但不能假装 Linux 内核官方有一个包含所有用户态命令的清单。

## 小白友好规则

- 开头可以写 2 到 5 行轻量对话，但必须服务于概念，不要为了热闹而热闹。
- 每 3 到 5 个自然段要给读者一个停靠点：小结、文字图、示例、命令、表格或检查问题。
- 第一次出现英文缩写时，要解释全称和作用。
- 第一次出现配置字段时，要解释字段含义。
- 命令后面要写预期结果。
- 示例块优先用行内注释、字段表或正文说明关键点，不要自动追加大段解释表。
- 不假设读者已经懂 Kubernetes、Python、机器学习。
- 能画数据流就用文字图说明。
- 少写“显而易见”“很简单”这类话。对新手来说，不简单才是正常的。

## 语言风格

- 像一个有经验的运维师傅带新人，而不是像考试资料在背定义。
- 可以口语化，但不要油腻，不要复制别人的梗。
- 段落要短，长概念拆成多段讲。
- 多用“你可以把它理解成”“这一步其实是在做”“坏了通常先看”。
- 少用空泛形容词，比如“强大”“高效”“灵活”。要写强在哪里、高效在哪里、灵活在哪里。
- 每篇都要有 AIOps 视角：这项技术如何进入指标、日志、链路、告警、自动化、模型或知识库。

## AIOps 关联规则

每篇文章必须明确它属于 AIOps 链路的哪一段：

- 数据采集：Prometheus、OpenTelemetry、Loki、Filebeat。
- 数据存储：Elasticsearch、ClickHouse、VictoriaMetrics、MySQL。
- 数据展示：Grafana。
- 告警治理：Alertmanager、Grafana Alerting。
- 自动化执行：Ansible、Runbook Automation、GitHub Actions。
- 智能分析：pandas、scikit-learn、LLM、RAG。
- 稳定性方法：SLI、SLO、RCA、事件响应、变更管理。

## 学习证据规则

每学完一个技术，至少提交一种证据：

- 配置文件：如 `prometheus.yml`、`compose.yaml`、dashboard JSON。
- 截图：如 targets 页面、Grafana dashboard、构建成功页面。
- 笔记：如“Counter 和 Gauge 的区别”。
- 排障记录：如“为什么容器里不能访问 localhost:9090”。
- 小项目：如“用 Prometheus + Grafana 监控一个 FastAPI demo”。

## 大厂面试级深度标准

后续所有技术栈都按“零基础能入门、生产问题能分析、大厂面试能承受连续追问”的标准编写。这里的“大厂面试级”不是承诺读完就拿到 offer，而是要求文章覆盖平台工程、SRE、DevOps、AIOps 岗位常见的原理、取舍和故障分析深度。

每篇文章必须同时提供两层学习路径：

```text
基础层
  -> 知道它解决什么问题
  -> 能跑通最小实验
  -> 能读懂核心配置和状态
  -> 能处理高频故障

进阶层
  -> 能画出请求或数据路径
  -> 能解释状态、一致性和控制机制
  -> 能设计生产高可用方案
  -> 能分析容量、性能、安全和升级风险
  -> 能在连续追问中说明证据、取舍和边界
```

### 生产深度检查项

只要该技术存在对应概念，文章就必须覆盖：

| 维度 | 必须回答的问题 |
|---|---|
| 内部机制 | 一次请求、一次写入或一次状态变化经过哪些组件？ |
| 状态与一致性 | 谁保存状态，如何并发控制，失败后如何收敛？ |
| 高可用 | 单点在哪里，多副本如何工作，故障域怎么划分？ |
| 容量与性能 | 核心容量指标是什么，瓶颈通常落在哪一层？ |
| 安全 | 身份、权限、加密、审计和最小权限如何落地？ |
| 变更 | 安装、扩容、缩容、升级、回滚和兼容性怎么处理？ |
| 可观测性 | 哪些指标、日志、事件和链路能证明系统健康？ |
| 故障模式 | 至少覆盖配置错误、依赖故障、资源耗尽和网络异常中适用的类型。 |
| 选型取舍 | 和相邻技术相比，什么时候选它，什么时候不选？ |

### 实验必须分两级

1. 基础实验：让第一次接触的读者完成安装、核心操作和结果验证。
2. 故障实验：主动制造一个可回收的错误，完成“现象 -> 证据 -> 假设 -> 验证 -> 修复 -> 清理 -> 复盘”。

实验不能碰真实生产环境。涉及删除数据、成员变更、证书、网络策略、集群升级等高风险动作时，必须显著写明实验边界、备份和回滚方法。

### 面试题必须带答案梯度

面试部分不能只罗列题目。每个核心主题至少给出一组递进追问：

```text
第一问：它是什么？
  -> 先用 30 秒讲清定义和价值

第二问：它怎么工作？
  -> 画出组件和数据路径

第三问：为什么这样设计？
  -> 说明一致性、性能、可用性或安全取舍

第四问：坏了怎么查？
  -> 按证据顺序排查，不直接猜根因

第五问：生产怎么设计？
  -> 给出容量、高可用、变更和回滚方案
```

文章至少要提供：一段 30 秒回答、一段 3 分钟回答、核心题参考答案、连续追问、一个系统设计题和一个事故复盘题。

### 质量验收

一篇文章只有同时满足下面条件，才能标记为“大厂面试级第一版”：

- 小白不依赖隐藏前置知识也能完成基础实验。
- 能解释核心组件之间的调用或数据路径。
- 能说清至少三个设计取舍，不只背定义。
- 能完成一次故障注入并保留证据。
- 能给出生产高可用、容量、安全、升级和回滚检查单。
- 面试题包含参考答案和递进追问。
- 学习证据可以形成 GitHub 项目、故障复盘或架构说明。

## 示例文章

当前第一批示范文章：

- [Prometheus 精讲](./observability/prometheus.md)
- [Grafana 精讲](./observability/grafana.md)
- [Docker Compose 精讲](./cloud-native/docker-compose.md)
- [Kubernetes 大厂面试级精讲](./cloud-native/kubernetes.md)

后续扩展其他技术栈时，以本标准和这些示范文章作为质量基线。
