# 华为 FusionSphere / FusionCompute 技术栈深讲

> 学习目标：从零分清 FusionSphere 产品族、FusionCompute、FusionSphere OpenStack、DCS 与 FusionCube；能画出 VRM/CNA、虚拟机、网络、存储和管理面的完整链路，完成只读资产与容量检查，并用证据定位管理面、计算、网络、存储、HA、升级和 License 类问题。

## 官方资料

- [华为 DCS 数据中心虚拟化解决方案](https://e.huawei.com/cn/solutions/storage/virtualization/datacenter-virtualization)
- [FusionCompute 官方文档入口](https://info.support.huawei.com/enterprise/zh/scale-out-storage/fusioncompute-pid-8576912/)
- [华为 OpenStack 百科与 FusionSphere OpenStack 说明](https://info.support.huawei.com/info-finder/encyclopedia/zh/OpenStack.html)
- [FusionCompute Web 客户端登录说明](https://info.support.huawei.com/hedex/api/pages/EDOC1100331202/FEM1012J/09/resources/en-us_topic_0292458982.html)
- [FusionCompute 添加数据存储](https://info.support.huawei.com/hedex/api/pages/EDOC1100413637/FZN1022J/01/resources/zh-cn_topic_0000001497798293.html)
- [VRM 主备心跳告警 ALM-650005](https://info.support.huawei.com/hedex/api/pages/EDOC1100413634/FEN1022J/02/resources/en-us_topic_0000001195953091.html)
- [虚拟机反复重启告警 ALM-650022](https://info.support.huawei.com/hedex/api/pages/EDOC1100413637/FZN1022J/01/resources/zh-cn_topic_0000001150033306.html)
- [华为 OceanProtect 数据保护](https://e.huawei.com/cn/products/storage/oceanprotect)
- [华为 FusionCube HCI](https://e.huawei.com/cn/products/storage/hci/fusioncube-1000)

> 版本快照：2026-08-31。FusionCompute 支持页公开存在 8.7、8.8、8.9 等版本资料，这只能证明相应文档存在，不能证明某现场运行这些版本，也不能单凭数字断言“最新版”。现场必须核对产品全名、完整版本、SPC/SPH、构建号、配套表、License 和维保材料。

## 先说清学习边界

商业虚拟化平台的安装包、License、内部修复命令和完整版本文档可能需要合同与授权。本篇提供：

1. 跨版本稳定的产品地图和工作机制。
2. 日常运维可执行的只读观察方法。
3. 不依赖商业软件的合成证据实验。
4. 生产故障的证据顺序、风险判断和原厂升级边界。

本篇不会编造默认密码、内部服务名、端口或跨版本修复命令，也不会把 KVM/OpenStack 通用知识冒充 FusionCompute 实测。

## 官方知识地图

```text
产品与版本
  -> FusionSphere / FusionCompute / FusionSphere OpenStack / DCS / FusionCube
  -> 版本、SPC/SPH、构建号、配套表、License、生命周期

虚拟化控制面
  -> VRM 主备、浮动管理地址、任务、事件、告警、权限、API/SNMP

计算数据面
  -> CNA、集群、资源池、主机、虚拟机、虚拟 CPU/内存、调度、HA、迁移

网络
  -> vNIC、端口组、虚拟交换、VLAN、CNA uplink、物理交换和网关

存储
  -> 虚拟磁盘、数据存储、适配器、多路径、LUN/分布式存储、快照、备份、容灾

生产运维
  -> 容量、性能、安全、证书、时间、升级、回滚、审计、AIOps、事故响应
```

推荐顺序：

```text
先分清产品和版本
  -> 再理解 VRM/CNA
  -> 再画 VM 创建链
  -> 再拆计算、网络、存储
  -> 再学 HA、备份、容灾
  -> 再做只读巡检和故障演练
  -> 最后学习升级、License、原厂支持和面试设计题
```

## 场景开场

凌晨，业务说“虚拟机重启了三次，系统一直没恢复”。平台上同时出现：

```text
VRM：虚拟机反复重启告警
CNA：一台主机短时断连
存储：某条路径抖动
网络：没有明显丢包告警
业务：数据库连接仍失败
```

如果只看到虚拟机现在是 Running 就宣布恢复，会漏掉两个事实：虚拟机可能仍在不稳定重启；客户机起来也不代表数据库、IP、挂载和应用顺序已经正确。

FusionSphere 排障的关键不是会点“重启”，而是把控制面任务、CNA 执行、网络、存储、客户机和业务验证放在同一条时间线上。

## 一句话人话版

FusionCompute 是华为服务器虚拟化平台：VRM 统一管理资源和任务，CNA 在计算节点上真正运行虚拟机，并把服务器、网络和存储组织成可管理的资源池。

## 小白先问

### FusionSphere 和 FusionCompute 是一个东西吗

不能简单画等号。FusionSphere 在不同年代和材料中可能指产品族或不同云形态；FusionCompute 是服务器虚拟化主线。现场先看产品全名、登录页面、版本和 BOM。

### FusionSphere 底层是不是 OpenStack

FusionSphere OpenStack 是华为基于 OpenStack 增强的企业形态；FusionCompute 的核心运维主线是 VRM/CNA。不能把所有写着 FusionSphere 的环境都判断成 OpenStack。

### VRM 挂了，虚拟机都会关机吗

已运行虚拟机通常由 CNA 数据面继续承载，但集中管理、任务、部分调度和故障处置能力会受影响。具体行为以目标版本和故障范围为准。

### 开了 HA 就不用备份了吗

不行。HA 主要处理宿主机故障后的虚拟机恢复；误删、数据损坏、勒索、历史恢复和异地灾难需要备份/复制/容灾。

## 产品族边界

| 名称 | 人话定位 | 本篇怎样处理 | 现场要核对 |
|---|---|---|---|
| FusionSphere | 历史和方案资料中的云计算/虚拟化产品族名 | 总称，不直接推断组件 | 产品全名、版本、部署形态 |
| FusionCompute | 服务器虚拟化与集中管理平台 | 本篇主体 | VRM/CNA 版本、补丁、集群 |
| FusionSphere OpenStack | 基于 OpenStack 增强的云平台形态 | 链接 OpenStack 原理 | 华为发行版组件和版本文档 |
| DCS | 当前公开的数据中心虚拟化解决方案 | 用于理解全景 | 实际购买的管理、备份、容灾模块 |
| FusionCube | 超融合基础设施 | 相邻方案 | 计算、存储、管理组件和型号 |
| eDME/DME | 全栈资源管理与智能运维 | 仅现场存在时展开 | 版本、纳管范围、接口和权限 |
| UltraVR | 虚拟机容灾管理与编排 | DR 专题 | 保护组、复制、RPO/RTO 和演练 |

公开方案出现一个组件，不等于现场已经采购、安装或启用。

## 核心架构：VRM 与 CNA

### VRM 五件套

是什么：VRM 是 Virtual Resource Management，虚拟资源管理控制面。

为什么需要：多台计算节点、网络、存储和虚拟机需要统一清单、权限、任务、策略和告警。

怎么工作：管理员或 API 向 VRM 提交意图，VRM 校验资源与权限，选择目标并向 CNA/相关资源执行，最后汇总任务、事件和状态。

怎么观察：Web 客户端查看集群、主机、虚拟机、任务、事件、告警、容量与性能；受控环境可使用版本匹配的 API/SNMP。

坏了怎么查：区分浮动地址、单节点地址、主备关系、管理网络、DNS/时间、证书、数据库空间、服务状态和最近变更；禁止同时重启主备。

### CNA 五件套

是什么：CNA 是 Computing Node Agent，部署在计算节点上的虚拟化执行面。

为什么需要：真正的 CPU/内存调度、虚拟网络、虚拟磁盘和虚拟机生命周期必须在宿主机执行。

怎么工作：接收管理任务，调用本地虚拟化、网络和存储能力创建或运行虚拟机，并上报状态。

怎么观察：从 VRM 看主机状态、资源、事件和性能；在授权 SOP 下检查主机硬件、链路和版本匹配信息。

坏了怎么查：先看单主机还是多主机、管理心跳还是数据面、硬件/BMC、网络、存储路径、资源耗尽和最近维护。

### 控制面与数据面

```text
管理面：管理员 -> VRM -> 任务/策略/告警
数据面：业务 -> VM -> CNA -> 网络/存储/物理硬件
```

VRM 页面可用不证明业务数据面健康；VRM 页面暂时不可用也不等于所有运行中 VM 已停止。事故声明要分别验证。

## 资源对象模型

```text
数据中心/站点
  -> 资源池或集群
     -> CNA 主机
        -> 虚拟机
           -> vCPU / 内存 / vNIC / 虚拟磁盘
     -> 虚拟网络/端口组
     -> 数据存储
```

| 对象 | 关键状态 | 容量问题 | 事故关联键 |
|---|---|---|---|
| 集群 | 可用主机、HA/调度策略 | 故障冗余和可售容量 | cluster ID |
| CNA | 在线、维护、告警 | CPU/内存/网卡/路径 | host ID/序列号 |
| VM | 运行、电源、任务 | vCPU/内存/磁盘 | VM ID/业务映射 |
| 数据存储 | 在线、可访问、容量 | 使用率、时延、路径 | datastore ID/LUN |
| 网络 | 端口组、VLAN、uplink | 带宽、队列、错误 | network/port/VM ID |
| 任务 | 排队、执行、成功、失败 | 并发和超时 | task/request ID |

名称可重复或被修改，自动化和 AIOps 关联优先使用稳定 ID，并保留名称用于人读。

## 一台虚拟机的创建链

```text
管理员/自动化提交参数
  -> VRM 认证与授权
  -> 校验模板、资源池、主机容量、网络和数据存储
  -> 选择集群/CNA/数据存储
  -> 准备虚拟磁盘和网络端口
  -> CNA 创建虚拟硬件
  -> 从镜像/磁盘/网络启动
  -> 安装或调用客户机工具（若方案需要）
  -> VRM 汇总任务、事件、状态
  -> 业务探针验证应用
```

页面显示“任务已提交”只是控制面受理，不代表所有后端步骤完成。

### 创建失败怎样定位停点

1. 保存任务 ID、对象 ID、时间、操作者和错误码。
2. 看卡在资源选择、磁盘、网络、创建还是启动。
3. 检查配额/容量与故障冗余，不只看总量。
4. 查目标 CNA、数据存储、网络和镜像同时间告警。
5. 找最近变更：维护、固件、交换、存储、补丁、策略。
6. 只在确定失败阶段后执行最小修复；不反复点创建制造残留。

## 计算虚拟化与资源争用

vCPU 不是独占物理核，虚拟内存也不等于永久独占物理内存：

```text
应用线程
  -> 客户机 OS 调度到 vCPU
  -> CNA/Hypervisor 调度到物理 CPU
```

客户机 CPU 40% 仍可能慢，因为 vCPU 在宿主机等待。容量要同时看：分配、实际使用、峰值、争用、故障后余量和增长。

### 容量四本数

| 数字 | 含义 | 误区 |
|---|---|---|
| 物理总量 | 安装的 CPU/内存/存储 | 忽略系统与故障预留 |
| 已分配 | 给 VM/资源的逻辑量 | 不等于同时真实消耗 |
| 实际使用 | 当前/区间真实负载 | 瞬时低不代表可过度出售 |
| 可承诺余量 | 满足 HA、维护和增长后可用 | 不能用“总量-已用”直接代替 |

生产设计至少按一台主机故障或计划维护后仍满足关键负载计算，而不是集群平时刚好不满。

## 网络完整路径

```text
应用 socket
  -> 客户机协议栈
  -> vNIC
  -> 虚拟交换/端口组
  -> CNA 物理 uplink/绑定
  -> TOR/汇聚交换机
  -> VLAN/路由/安全设备
  -> 目标服务
```

管理、业务、存储、迁移等平面应按现场设计隔离。VLAN、MTU、绑定、网关和端口不能跨版本硬背。

### VM 有 IP 但不通

1. 客户机：IP、掩码、网关、路由、DNS、防火墙、监听端口。
2. 虚拟层：vNIC 是否连接、端口组/VLAN 是否正确、策略是否放行。
3. CNA：uplink、绑定成员、错误/丢包、MTU。
4. 物理网：交换端口、trunk、VLAN、路由、ACL/防火墙。
5. 对端：服务监听、返回路由、负载均衡和应用日志。

不要把“能 ping”当业务正常，也不要关闭全网安全策略试错。

## 存储完整路径

```text
应用读写
  -> 客户机文件系统/卷管理
  -> 虚拟磁盘
  -> CNA 存储协议与多路径
  -> SAN/NAS/分布式存储网络
  -> 阵列/存储池/磁盘
```

FusionCompute 添加数据存储前需要识别适配器和设备。设备扫描不到时，应检查分区呈现、SAN zoning、iSCSI/NFS 网络、认证、多路径、驱动/固件和兼容矩阵，而不是一直刷新。

### I/O 慢的证据顺序

1. 业务操作和时间窗。
2. 客户机延迟、队列、错误和文件系统。
3. 虚拟磁盘与目标数据存储。
4. CNA 路径、HBA/NIC、队列和错误。
5. 交换网络/存储端口。
6. 阵列/分布式存储容量、缓存、热点、重建和磁盘故障。
7. 同时段迁移、快照合并、备份和扩容。

## 快照、HA、备份和容灾

| 能力 | 解决什么 | 不解决什么 |
|---|---|---|
| 快照 | 短期回退某个虚拟磁盘状态 | 长期独立备份、存储故障 |
| VM HA | 宿主机失败后在其他资源上恢复 VM | 应用零中断、数据逻辑损坏 |
| VRM 主备 | 管理控制面冗余 | 业务跨站容灾 |
| 备份 | 独立恢复点和历史恢复 | 自动接管全部业务 |
| 存储复制/双活 | 数据跨故障域保护 | 应用顺序、DNS、依赖编排 |
| UltraVR/DR 编排 | 保护组、切换、回切和演练 | 未配置的业务依赖和人为验证 |

RPO（允许丢多少数据）和 RTO（允许停多久）是经验证的业务目标，不是看产品页后自动成立。用最近一次恢复/切换演练报告证明。

## VRM 主备与心跳故障

集群部署通常通过浮动管理地址访问。VRM 主备心跳中断会降低可靠性，甚至带来数据不一致风险。

证据顺序：

1. 当前谁是主、谁是备、浮动地址在哪里。
2. 两节点各自状态和管理网络双向连通。
3. NTP/时间偏差、证书有效期和名称解析。
4. 数据库空间、磁盘和关键服务状态。
5. 最近网络、证书、密码、恢复或补丁变更。
6. 原厂告警 ID、版本文档和工单。

红线：未确认角色和一致性前，不同时重启、不强制双主、不删除同步状态、不随意倒换。

## 版本、补丁、License 与三本账

### 资产账

服务器、CPU、内存、NIC、HBA/RAID、交换机、存储、集群、故障域和业务映射。

### 软件账

```text
产品全名
  + 主版本
  + SPC/SPH/补丁
  + 构建号
  + 组件版本
  + 配套表/兼容矩阵
  + 安装/升级日期
```

### 授权与服务账

License 文件/容量/功能、到期规则、原厂维保有效期、升级权益、厂商联系人和升级路径。

没有证明时使用“据现场反馈，待核对授权证明和维保合同”，不要写成已确认事实。

## 升级与回滚

升级不是把 VRM/CNA 包换成更大版本：

```text
当前完整版本与拓扑
  -> 目标版本与升级路径
  -> 硬件/固件/驱动/存储/网络/备份配套表
  -> 健康预检查和容量余量
  -> 配置/数据库/关键业务备份
  -> 维护窗、顺序、停止条件
  -> 分阶段升级
  -> 平台和业务双重验收
  -> 观察期
```

“回滚”能否原地完成取决于版本。数据库和配置已迁移后，可能只能恢复备份或按原厂方案回退。变更前必须把回退入口、最大允许时间和数据影响写清。

应升级原厂的情况：闭源控制面内部错误、主备不一致、补丁签名/BOM、跨版本回退、License 重签、数据存储一致性、容灾反向同步和厂商专用工具操作。

## 安全边界

1. 管理网与业务网隔离，管理入口经过堡垒机/受控终端。
2. 按角色最小权限，日常只读、变更、审计和紧急账号分开。
3. 密码、证书、API 凭据进入安全保管，不写脚本和仓库。
4. 时间同步是证书、审计和主备判断基础。
5. 高危操作双人复核，记录任务 ID、审批单、影响和回滚。
6. SNMP/API 使用专用只读账号、限速和来源限制。
7. 导出的支持包、清单和截图脱敏后才可离开受控环境。

## 可观测性与 AIOps

### 必采信号

| 信号 | 示例 | 用途 |
|---|---|---|
| 指标 | 主机/VM CPU、内存、存储时延、网络错误、容量 | 趋势与范围 |
| 告警 | VRM 心跳、CNA 状态、VM 重启、存储路径 | 触发事件 |
| 任务/事件 | 创建、迁移、重配置、维护、HA | 解释状态变化 |
| 日志 | 控制面、CNA、网络、存储、认证 | 定位阶段和错误 |
| 审计 | 操作人、对象、时间、结果 | 关联人为变更 |
| 拓扑 | 业务-VM-CNA-网络-存储 | 爆炸半径与根因 |

推荐关联键：告警 ID、任务 ID、VM ID、host ID、datastore ID、网络 ID、变更单和统一时间窗。

### AIOps 只读诊断流程

```text
告警进入
  -> 补齐对象与拓扑
  -> 关联同时间任务/变更
  -> 检查同故障域其他对象
  -> 计算容量和趋势
  -> 输出假设与下一条只读证据
  -> 人工审批修复
  -> 业务探针验证和复盘
```

自动化不直接重启主备、迁移全场 VM 或清理存储状态。

## 操作与证据字典

| 操作 | 目的 | 正常结果 | 异常先看 | 风险 |
|---|---|---|---|---|
| 查看版本/补丁 | 建软件账 | 完整版本可记录 | 控制台、升级报告、工单是否一致 | 只读 |
| 查看 License | 建授权账 | 功能/容量/期限明确 | 合同、授权证明 | 只读但需脱敏 |
| 查看 VRM 主备 | 判断控制面冗余 | 主备关系和浮动地址正常 | 网络、时间、证书、空间 | 不强制倒换 |
| 查看任务/事件 | 找状态停点 | ID、阶段和错误可追踪 | 同时段依赖告警 | 只读 |
| 查看容量 | 做扩容/风险判断 | 含预留和 HA 余量 | 分配与使用不要混淆 | 只读 |
| 进入维护模式 | 计划主机维护 | VM 已安全迁移/处理 | 容量、亲和性、存储/网络 | 变更操作 |
| 创建/删除快照 | 短期变更保护 | 快照链受控并按时清理 | 空间、合并时间、备份冲突 | 高风险 |
| 升级/补丁 | 修复和生命周期 | 全阶段与业务验收通过 | 配套表、健康检查、回退 | 原厂/授权变更 |

## 基础实验：生成私有云只读证据报告

仓库提供 [private-cloud-evidence-lab](https://github.com/quweisheng/zero-to-aiops/tree/main/examples/private-cloud-evidence-lab)。实验只读取合成 JSON，不连接真实 FusionCompute。

```powershell
cd examples\private-cloud-evidence-lab
powershell -ExecutionPolicy Bypass -File .\Invoke-PrivateCloudAudit.ps1 `
  -InputPath .\fixtures\healthy.json `
  -OutputPath .\evidence\healthy-report.md
```

预期：退出码 0，报告显示 PASS，包含 FusionSphere 产品形态、版本证据、VRM 管理节点、计算/内存/数据存储容量和人工确认清单。

失败先查：工作目录、输入 JSON 编码/结构、输出目录权限。报告是合成教学证据，不能说成 FusionCompute 实测。

清理：删除自己生成的 `evidence/` 报告；不要删除 fixtures。

## 故障实验：VRM 备节点离线与容量越线

```powershell
powershell -ExecutionPolicy Bypass -File .\Invoke-PrivateCloudAudit.ps1 `
  -InputPath .\fixtures\degraded.json `
  -OutputPath .\evidence\degraded-report.md
```

预期：退出码 2，报告指出 standby offline 与内存/CPU 容量风险。

完整证据链：

1. 现象：总体 DEGRADED。
2. 证据：具体角色状态、资源 used/total/percent。
3. 假设：管理冗余下降；故障/维护后容量不足。
4. 验证：比较健康 fixture，不修改生产。
5. 修复思路：现场先只读核对网络、时间、证书和容量；闭源内部问题升级原厂。
6. 回滚：使用健康 fixture 恢复实验。
7. 复盘：补阈值依据、负责人、扩容/治理和验证日期。

## 有授权环境时的只读实验

1. 导出或人工记录产品全名、版本、补丁、构建号、License 和证据来源。
2. 记录 VRM 主备、集群、CNA、VM、数据存储、网络和活动告警摘要。
3. 选择一个非敏感告警，保存 ID、对象、时间和关联任务。
4. 沿 VM -> CNA -> 网络/存储 -> 业务探针形成证据链。
5. 报告脱敏：不含账号、密码、内网 IP、真实业务名和原始日志。

## 高频故障矩阵

| 现象 | 首轮证据 | 假设方向 | 禁止先做 |
|---|---|---|---|
| VRM 页面打不开 | 浮动/节点地址、主备、证书、时间、网络 | 入口、主备、服务、空间 | 同时重启两节点 |
| 主备心跳中断 | ALM-650005、双向连通、NTP、证书 | 网络、时间、数据库空间 | 强制双主/盲目倒换 |
| VM 创建失败 | task ID、容量、CNA、网络、存储、镜像 | 资源或依赖失败 | 反复点击创建 |
| VM 反复重启 | ALM-650022、CNA、存储、镜像、操作审计 | 路径、镜像、主机、误操作 | 只查客户机应用 |
| VM 无网络 | vNIC、端口组、VLAN、uplink、物理网 | 配置/链路/返回路径 | 全放通策略 |
| I/O 慢 | 客户机、虚拟盘、路径、存储端 | 热点、重建、链路、快照/备份 | 批量迁移所有 VM |
| HA 后应用未恢复 | VM、IP/DNS、挂载、数据库、顺序 | 依赖或数据一致性 | 报“VM Running 即恢复” |
| 升级失败 | 完整版本、配套表、预检、失败阶段 | 兼容、健康、包/组件 | 删升级状态/跨版本套命令 |

## 生产设计题

题目：设计一个承载核心数据库与中间件的 FusionCompute 集群。

回答主线：

1. 业务分级与 RPO/RTO，明确虚拟化 HA 和应用高可用的边界。
2. VRM 主备与管理网故障域；CNA 按机柜/供电/交换/存储故障域布局。
3. N+1 或更高容量余量，考虑维护和故障后的峰值。
4. 网络平面、冗余 uplink、VLAN/MTU 和物理交换一致性。
5. 数据存储、多路径、阵列/分布式存储保护和独立备份。
6. 最小权限、堡垒机、证书、NTP、审计和只读采集。
7. 版本/BOM/License/维保三本账，升级和回退演练。
8. 用业务探针、平台指标、告警、任务、日志和拓扑验收。

追问：vCenter/vSphere 与 FusionCompute 怎么选？不要只比功能表，应比较现有技能、硬件/存储/备份生态、许可、交付、升级、API、支持和退出成本。

## 事故复盘题

题目：一台 CNA 断连后 VM 由 HA 拉起，但数据库仍不可用。

回答：先限制影响和冻结不必要变更；建立时间线，确认 HA、存储、IP/DNS、数据库恢复和依赖顺序；业务验证不是看电源状态。若存储出现双写/一致性风险，停止扩大写入并按容灾 SOP/原厂支持处理。恢复后补应用级探针、依赖启动编排和演练。

## 面试怎么讲

### 30 秒版本

FusionCompute 是华为服务器虚拟化平台，VRM 是统一管理控制面，CNA 是运行虚拟机的计算执行面。一次 VM 创建还依赖网络、数据存储和镜像。生产运维要区分管理面与数据面、HA 与备份/容灾，并用任务 ID、对象 ID、告警、拓扑和变更记录排障。版本、补丁、License 和升级必须以现场证据及目标版本原厂文档为准。

### 3 分钟版本

先澄清 FusionSphere 是产品族称呼，主线是 FusionCompute；画出管理员 -> VRM -> CNA -> 虚拟机/网络/存储。再讲 VRM 主备、资源对象、VM 创建链、计算争用、网络和存储路径。生产上用故障域和 N+1 设计集群，用独立备份/DR 保护数据，用最小权限、证书、NTP 和审计保护管理面。排障按任务、告警、对象、依赖和业务探针收证据；闭源控制面、主备一致性、补丁、License 和跨版本回退及时升级原厂。

### 连续追问

1. **VRM 和 CNA 区别？** VRM 管意图和状态，CNA 执行虚拟机数据面。
2. **VRM 挂了为何 VM 还能跑？** 管理控制面与已运行数据面分离，但集中管理和部分调度受影响。
3. **HA 等于零中断吗？** 不等于，VM 重启和应用恢复有时间，还要处理依赖与数据。
4. **快照能否代替备份？** 不能，通常依赖原数据存储且长期快照影响容量/性能。
5. **创建 VM 失败怎么查？** 拿任务 ID 沿 VRM、资源、CNA、网络、存储、镜像定位停点。
6. **升级怎么回滚？** 先按目标版本文档定义能否原地回退，否则靠配置/数据库备份和完整恢复方案。

## 学习检查清单

- [ ] 能区分 FusionSphere、FusionCompute、FusionSphere OpenStack、DCS、FusionCube。
- [ ] 能解释 VRM/CNA 与控制面/数据面。
- [ ] 能画 VM 创建、网络和存储三条链。
- [ ] 能区分分配、使用、可承诺容量和故障余量。
- [ ] 能区分 HA、VRM 主备、快照、备份、复制和 DR。
- [ ] 能建立资产、软件、授权服务三本账。
- [ ] 能按证据排查 VRM、CNA、VM、网络和存储故障。
- [ ] 能说明升级预检查、兼容矩阵、停止条件和回退边界。
- [ ] 能运行合成基础/故障实验并正确声明验证边界。
- [ ] 能说出何时必须升级华为原厂或授权专家。

## GitHub 学习证据

```text
fusionsphere-learning/
  architecture/
    vrm-cna-data-path.md
  inventory/
    version-license-template.md
    sanitized-capacity.json
  evidence/
    readonly-audit.md
    alert-timeline.md
  experiments/
    healthy-report.md
    degraded-report.md
  postmortem/
    vrm-heartbeat-or-vm-restart.md
```

所有材料必须写“合成实验”或“脱敏只读证据”，不得提交真实 IP、域名、账号、License、序列号、业务名、日志和配置秘密。

## 下一步

- 学通用虚拟化：[KVM](./kvm.md)、[VMware vSphere](./vsphere.md)。
- 现场若是 FusionSphere OpenStack：继续 [OpenStack](./openstack.md)，再按华为目标版本资料核对差异。
- 学私有云全栈：[Tencent TCE](./tencent-tce.md)。
